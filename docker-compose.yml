version: "3.8"

services:
  virtoffice:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9502:9502"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9502/"]
      interval: 30s
      timeout: 10s
      retries: 3
